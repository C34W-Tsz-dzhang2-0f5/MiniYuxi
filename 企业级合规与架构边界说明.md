# MiniYuxi 深度构造 · 企业级合规与架构边界说明

> 生成时间：2026-09-14
> 对应需求：运行时热切换与多供应商路由 / MCP 原生适配层 / 等保·ISO·CMK·SOC 审计 / 多 Agent 协同与工作流自动化 / 多租户 SaaS 与高并发

## 一、先讲清的红线：代码能交付什么，不能交付什么

| 诉求 | 性质 | 代码能否"交付" |
|---|---|---|
| 等保三级**认证** | 监管认证（证书） | ❌ 不能。需测评机构现场评估 + 公安/网信备案，证书由机构出具 |
| ISO 27001**认证** | 认证（证书） | ❌ 不能。需认证机构审核 + 证书，代码交付不了 |
| CMK 客户托管密钥**合规背书** | 依赖外部密钥管理 | ❌ 证书/合规结论不能。代码提供开关与钩子，密钥本身需 KMS/HSM |
| 对外高并发 + 多租户 SaaS | 基础设施架构 | ⚠️ 部分。代码交付隔离闸门与无状态接口；水平扩展需负载均衡+托管库 |

**本批次交付的是"技术控制层 + 架构骨架"**：让未来测评/认证能过的控制点已就位、可验证、可审计；但证书与 HA 生产环境必须走外部流程与基础设施，任何代码平台都无法替代。

## 二、本批次已落地的代码（5 模块 + 17 端点 + TDD）

| 项 | 模块 | 文件 | 真实能力 |
|---|---|---|---|
| 1 热切换/多供应商 | `core/provider_router.py` | llm_providers / kv_store 表 | 供应商注册表、运行时 `set_active` 热切换、故障转移（主→备用→离线兜底） |
| 2 MCP 原生适配 | `core/connectors.py` | connectors 表 | 微信/企微/CRM/ERP/飞书 5 类适配器框架、健康探测、未配置优雅降级、发送抽象（留好真实厂商 API 缝） |
| 3 SOC 级审计 | `core/soc_audit.py` | audit_events 表 | 结构化事件 + **哈希防篡改链** + 篡改检测 + 查询/导出 |
| 4 多 Agent 协同 | `core/orchestration.py` | 复用 canvas/subagent | 多角色专家组闭环（生成→自评→协调器裁决）+ 工作流自动化（复用画布 DAG） |
| 5 多租户合规 | `core/multitenant.py` | tenant_policies 表 | 租户配额/驻留/MLPS/CMK 闸门 `enforce` + **合规就绪度账本** `readiness` |

验证：单元测试 `test_deep.py` 全绿（5 模块、含故障转移/哈希链篡改检测/配额拒绝）；`selftest.py` **20/20 未退**；三指纹红线 `7d7e3b25 / c1b265e3 / 10f8e385` **未变**；生产 8801 端到端 17 端点冒烟全 200。

## 三、各认证/架构诉求的"代码控制点 vs 外部依赖"对照

### 等保三级（MLPS L3）
- **代码已具备（code_done）**：RBAC 角色矩阵、统一身份鉴别（哈希+salt+Token TTL）、SOC 审计哈希链、租户数据隔离闸门、敏感动作 HITL 审批卡。
- **需外部（external_required）**：测评机构现场评估 + 公安/网信备案、安全边界/网络区域划分（拓扑与设备）、IDS/日志集中审计硬件、年度复测。

### ISO 27001
- **代码已具备**：审计留痕、访问控制、CMK 开关字段。
- **需外部**：认证机构审核与证书、ISMS 制度文件与内审、管理层承诺与风险处置。

### CMK 客户托管密钥
- **代码已具备**：`tenant_policies.cmk_enabled / cmk_key_id` 字段与开关、落盘加密钩子接口预留。
- **需外部**：对接云厂商 KMS 或自建 HSM、密钥轮换流程。

### 多租户 SaaS + 对外高并发（HA）
- **代码已具备**：无状态请求级 `db.connect`、按 `tenant_id` 路由、KV/供应商可持久化、配额闸门。
- **需外部**：负载均衡 + 多无状态实例（本机单进程不满足）、托管数据库/读写分离（SQLite 单文件不满足高并发写）、对象存储/向量库外置（sqlite-vec 进程内不满足）。

## 四、拿到真实认证的下一步路线（不在本批次代码范围）

1. **等保三级**：联系有资质的测评机构做差距分析 → 补安全边界/IDS/集中审计 → 整改 → 测评 → 公安备案。技术侧本平台已覆盖大部分控制点，可显著缩短整改周期。
2. **ISO 27001**：建 ISMS 制度 → 内审 → 选认证机构（如 SGS/BSI）→ 外审取证 → 年度监督。
3. **CMK**：在云账号开通 KMS，把 `cmk_key_id` 指向真实密钥，实现落盘加密；密钥轮换走云厂商能力。
4. **HA/SaaS**：容器化（Docker/K8s）→ 无状态多副本 + 负载均衡 → 数据库换托管 PG/云库 → 向量库外置（如 pgvector）。本平台接口已按无状态设计，改造量集中在存储层。

## 五、风险分级

- 🔴 **高（须阿长知悉）**：等保三级/ISO **证书不是代码产物**，对外宣称"已通过"属虚假陈述，有合规与法律风险；高并发对外服务在本机单进程架构下**不满足**，上线前必须补基础设施。
- 🟡 **中**：MCP 适配器目前是"框架 + 优雅降级 + 真实 API 缝"，未内置各厂商完整 SDK（需各自凭证与网络）；多 Agent 协同每次真实 LLM 调用约 40–85 秒（双角色），交互层需加异步/流式。
- 🟢 **低**：所有新增代码零新增依赖、数据不出司、三指纹与 selftest 基线未破；切换/审计/隔离等技术控制点已可举证。
